{
  "name": "pi-frontier",
  "version": "1.0.0",
  "description": "Fetch the models.dev catalog and surface frontier (latest) models per provider/tier using a release-date-first filter — no AI stage.",
  "type": "module",
  "scripts": {
    "fetch": "node src/fetch.js",
    "filter": "node src/filter.js",
    "frontier": "node src/frontier.js",
    "all": "npm run fetch && npm run filter && npm run frontier"
  },
  "keywords": ["llm", "models", "frontier", "models.dev"],
  "license": "MIT"
}
